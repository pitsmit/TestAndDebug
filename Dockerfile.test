# Dockerfile.test
FROM node:18-bullseye

RUN apk add --no-cache \
    git \
    openssh-client \
    openjdk11-jre \
    postgresql-client

WORKDIR /app

COPY Server/ ./
COPY shared/ ../shared/

RUN npm ci

CMD ["sh", "-c", "npm run test:ci"]